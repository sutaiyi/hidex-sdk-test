export function instructionReset(order) {
    const instruction = order || 'testInstruction';
    return instruction;
}
export function instructionsCheck(order) {
    const instruction = order || 'testInstruction';
    return instruction;
}
export function instructionsEdit(order) {
    const instruction = order || 'instructionsEdit';
    return instruction;
}
